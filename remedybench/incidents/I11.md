# Parser kill switch bypass

A risky parser path bypassed the intended kill switch and unsafe defaults remained active.

## Corrective actions

- Gate all parser entry points.
- Set parser concurrency feature flag default to 0.
- Prove operator tested the kill switch.
